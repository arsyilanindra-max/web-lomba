import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../App.jsx'
import '../index.css' // warning ignorable, runtime masi OK

ReactDOM.createRoot(document.getElementById('root')).render(
  <App />
)
